version: "3"

services:
  questiondb:
    image: postgres
    environment:
      POSTGRES_USER: ofyenn
      POSTGRES_PASSWORD: ofyenn
      POSTGRES_DB: questions
    volumes:
      - ./db:/var/lib/postgresql/data

  fastapi:
    build: 
      context: ./api
    ports:
      - "8000:80"
